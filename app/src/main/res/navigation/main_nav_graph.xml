<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    app:startDestination="@id/nav_wl_acl"
    android:id="@+id/main_nav_graph">

    <fragment
        android:id="@+id/nav_wl_acl"
        android:name="me.fb.ng.ctrl.ui.wlacl.WirelessAclFragment" >

        <action
            android:id="@+id/to_settings"
            app:destination="@id/nav_settings" />

    </fragment>

    <fragment android:id="@+id/nav_settings"
        android:name="me.fb.ng.ctrl.ui.settings.SettingsFragment" />

</navigation>